using System;
using ModelContextProtocol.Protocol.Types;

// Тестовый файл для проверки доступных типов из ModelContextProtocol
namespace Test
{
    public class TestTypes
    {
        public void TestProgressTypes()
        {
            // Попробуем использовать ProgressNotificationValue из ModelContextProtocol
            var progress = new ProgressNotificationValue();
        }
    }
}
